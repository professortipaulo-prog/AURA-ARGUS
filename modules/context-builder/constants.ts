/**
 * modules/context-builder/constants.ts
 */
export const DEFAULT_MAX_TOKENS = 4000;
/** Percentual reservado para fragmentos de memória dentro do orçamento total. */
export const MEMORY_TOKEN_BUDGET_RATIO = 0.5;
