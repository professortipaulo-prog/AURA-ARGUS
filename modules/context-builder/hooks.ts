/**
 * modules/context-builder/hooks.ts
 * Reservado para uso futuro em componentes que exibam "contexto usado"
 * na resposta da IA (transparência para o usuário). Sem lógica ainda.
 */
'use client';

export function useBuiltContextPlaceholder() {
  return { available: false };
}
