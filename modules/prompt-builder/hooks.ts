/**
 * modules/prompt-builder/hooks.ts
 * Reservado para inspeção/depuração do prompt final na UI de
 * desenvolvimento (ex: painel "ver prompt enviado"). Sem lógica ainda.
 */
'use client';

export function usePromptPreviewPlaceholder() {
  return { available: false };
}
