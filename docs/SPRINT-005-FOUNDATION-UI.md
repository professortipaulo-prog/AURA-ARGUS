# Sprint 005 — Foundation UI

## Status
Concluída como primeira interface utilizável do AURA/ARGUS.

## Entregas
- Landing page profissional em `app/page.tsx`.
- Login em `app/login/page.tsx`.
- Cadastro em `app/register/page.tsx`.
- Dashboard em `app/dashboard/page.tsx`.
- Layout interno com sidebar e header.
- Páginas internas: chat, projetos, documentos, memória, agentes, configurações e admin.
- Componentes reutilizáveis em `components/ui` e `components/layout`.
- Preparação de sessão em `lib/auth/session.ts`.

## Observação
A autenticação real com Supabase Auth será ativada na Sprint 006. Esta sprint prioriza estrutura visual, navegação e aderência arquitetural.
